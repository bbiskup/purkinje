sudo: required
dist: xenial
python:
  - "2.7"
cache:
  apt: true
  # TODO local node modules path correct?
  directories:
    - $HOME/data

before_install:
    - sudo apt-get update -yy
    - sudo apt-get install make
install:
    - make stack-up

script:
   - make test
   - make doc

# after_success:
#   make coveralls
